{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "rdbg",
      "name": "Debug Rails Server",
      "request": "launch",
      "script": "bin/rails",
      "args": ["server"],
      "cwd": "${workspaceFolder}"
    }
  ]
}